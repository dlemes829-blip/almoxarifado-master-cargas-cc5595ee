## Packages
framer-motion | Page transitions and layout animations
recharts | Dashboard analytics and stock charts
date-fns | Human-readable date formatting
jwt-decode | Parsing JWT tokens to extract user data

## Notes
- WebSocket connects to /ws for real-time notifications
- Using Bearer token in Authorization header for all API requests
- The default dark mode is enforced via CSS classes
